/** Injected by vite.config.ts `define` from scripts/site-url.mjs (SITE_URL → Netlify URL → fallback). */
declare const __SITE_URL__: string;
/** Injected by vite.config.ts `define` from MONITOR_API_URL (origin of ouro-monitor, no trailing slash). */
declare const __MONITOR_API__: string;
