{
  "name": "@ouro/analytics",
  "private": true,
  "type": "module",
  "description": "analytics.ourolayer.com — everything Ouro reads off Robinhood Chain: the airdrop meta (every tax-index token that pays its holders, side by side), the Reserve's ledger, and every $OURO payout.",
  "scripts": {
    "dev": "react-router dev --port 5174",
    "build": "react-router build && node scripts/postbuild.mjs",
    "preview": "sirv build/client --single --port 4174",
    "typecheck": "react-router typegen && tsc"
  },
  "dependencies": {
    "@ouro/ds": "workspace:*",
    "@ouro/monitor-client": "workspace:*",
    "isbot": "^5",
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "react-router": "^8",
    "viem": "^2.56.3"
  },
  "devDependencies": {
    "@react-router/dev": "^8",
    "@types/node": "^22",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "sirv-cli": "^3.0.1",
    "typescript": "^5.9.3",
    "vite": "^8.0.3"
  }
}
