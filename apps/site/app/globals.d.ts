/** Injected by vite.config.ts `define` from scripts/site-url.mjs (SITE_URL → Netlify URL → fallback). */
declare const __SITE_URL__: string;
/* __MONITOR_API__ is declared by @ouro/monitor-client, which is what reads it. This app still has to
   `define` it in vite.config.ts — see that file. */
/** Injected by vite.config.ts from WALLETCONNECT_PROJECT_ID. Public identifier; "" disables WalletConnect. */
declare const __WALLETCONNECT_PROJECT_ID__: string;
